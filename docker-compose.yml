version: "2.2"

services:
  fis-express:
    image: johnnythai04/fis-express
    ports: 
      - "4001:4001"
    env_file:
      - .env
  fis-webapp:
    image: johnnythai04/fis-webapp
    ports:
      - "3001:3001"
    env_file:
      - .env
